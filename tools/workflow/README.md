# Experiment Workflows

`train.py` safely starts or resumes one run. `evaluate.py` enforces validation
before the single confirmation-gated test pass. Aggregate NIR modules run six
jobs sequentially.
