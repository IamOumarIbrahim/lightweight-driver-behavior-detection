DATA WORKFLOW
Place DMD and Drive&Act under data, unpack if needed, prepare each track, optionally build Label Studio review media, then run each dataset check. NIR is always sampled to ten frames per one-second snippet.

01_unpack_sources keeps source archives. Use 01b only when you intentionally want successfully extracted archives deleted.
