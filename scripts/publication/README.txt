PUBLICATION FIGURES
Run build_figures.bat after aggregate result JSON is frozen. It creates IEEE-sized PDF and SVG figures plus 600-DPI PNG previews without reading checkpoints or active run directories.
