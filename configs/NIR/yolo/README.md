# YOLO NIR Configuration

Both YAML files use the same validation and test lists. Each one-second NIR
snippet contributes one deterministic midpoint frame at 1 FPS. Training uses the
protocol-wide 100-epoch maximum and validation-selected best checkpoint.
