PURPOSE
Machine-readable, frozen benchmark definitions.

CONTENTS
RGB and NIR contain track-specific dataset/model settings. backends.yaml pins external code and weights. patches contains the two audited accumulation patches. Do not change a frozen protocol after test access.
